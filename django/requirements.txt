Django==5.2.17
djangorestframework==3.18.0
PyJWT==2.13.0
cryptography==50.0.1
python-dotenv==1.2.2
dj-database-url==2.3.0
# Observabilidade (erro 500 não tratado) — mesmo provedor do Next.js, ver
# src/lib/rollbarServidor.ts; token próprio (ROLLBAR_..._DJANGO_SERVER_TOKEN_...)
rollbar==1.4.0
# Driver do Postgres (dev e produção — banco compartilhado local, ver DATABASE_URL no .env.example)
psycopg[binary]==3.3.4
pytest==8.4.2
pytest-django==4.12.0
